# תוכנית עבודה - פלטפורמת מילון שפת השקדול

## סקירה כללית
אתר קהילתי לפיתוח שפה חדשה בשם **שקדול**, בו משתמשים ממציאים מילים חדשות ומגדירים את משמעותן. המילים נכנסות למילון לאחר אישור ועדת השפה בראשות גב' שקד.

---

## ארכיטקטורה טכנית

### Stack טכנולוגי
| רכיב | טכנולוגיה | סיבה |
|-------|-----------|------|
| Frontend | **Next.js 14 (App Router)** | SSR, RTL מובנה, דפים דינמיים |
| Styling | **Tailwind CSS** | עיצוב מהיר, תמיכה ב-RTL |
| Backend/API | **Next.js API Routes** | אין צורך בשרת נפרד |
| Database | **PostgreSQL** (via Railway) | חינמי ב-Railway, אמין |
| ORM | **Prisma** | type-safe, מיגרציות קלות |
| Auth | **NextAuth.js** | הרשמה עם email, ניהול sessions |
| Content Moderation | **Gemini 2.0 Flash** (Lite/Bench) | LLM זול לבדיקת תוכן פוגעני |
| Hosting | **Railway** | deploy פשוט מ-GitHub |

---

## מבנה בסיס הנתונים (Prisma Schema)

### טבלת User (משתמשים)
- `id` - מזהה ייחודי (UUID)
- `email` - כתובת דוא"ל (unique)
- `fullName` - שם מלא
- `age` - גיל
- `role` - תפקיד (USER / ADMIN / COMMITTEE)
- `status` - מצב (ACTIVE / WARNED / SUSPENDED / BANNED)
- `warningCount` - מספר אזהרות (0-3)
- `suspendedUntil` - תאריך סיום השעיה (nullable)
- `termsAcceptedAt` - תאריך אישור תקנון (nullable, חובה להשלמת הרשמה)
- `dailySubmissions` - מונה הגשות יומי (מתאפס כל יום)
- `lastSubmissionDate` - תאריך הגשה אחרונה (לצורך איפוס מונה יומי)
- `createdAt` - תאריך הרשמה
- `updatedAt` - תאריך עדכון אחרון

### טבלת Word (מילים מוצעות)
- `id` - מספור אוטומטי
- `word` - המילה (עד 12 אותיות, **unique** - מניעת כפילויות)
- `meaning` - משמעות (עד 2 משפטים)
- `startingLetter` - האות שהוגרלה
- `status` - מצב (PENDING / APPROVED / REJECTED)
- `submittedById` - FK למשתמש שהציע
- `reviewedById` - FK למי שאישר/דחה (nullable)
- `reviewNote` - הערת ביקורת (nullable)
- `submissionTime` - זמן שלקח להגשה (בשניות, מתוך 60)
- `sessionToken` - טוקן סשן ההגשה (לאימות טיימר צד-שרת)
- `sessionStartedAt` - זמן תחילת הסשן בצד השרת (לאימות 60 שניות)
- `createdAt` - תאריך הגשה
- `updatedAt` - תאריך עדכון

### טבלת ModerationLog (לוג ניטור תוכן)
- `id` - מזהה
- `userId` - FK למשתמש
- `wordAttempt` - המילה שנחסמה
- `meaningAttempt` - המשמעות שנחסמה
- `reason` - סיבת החסימה (מ-LLM)
- `actionTaken` - פעולה שננקטה (WARNING / SUSPEND_24H / SUSPEND_10D / BAN)
- `createdAt` - תאריך

### טבלת Notification (התראות)
- `id` - מזהה
- `userId` - FK למשתמש
- `type` - סוג (WORD_APPROVED / WORD_REJECTED / WARNING / SUSPENSION / BAN)
- `title` - כותרת ההתראה
- `message` - תוכן ההתראה
- `wordId` - FK למילה הרלוונטית (nullable)
- `isRead` - האם נקראה (boolean, default: false)
- `createdAt` - תאריך

### טבלת AdminAuditLog (לוג פעולות אדמין)
- `id` - מזהה
- `adminId` - FK לאדמין שביצע את הפעולה
- `action` - סוג הפעולה (APPROVE_WORD / REJECT_WORD / SUSPEND_USER / BAN_USER / CHANGE_SETTING / etc.)
- `targetType` - סוג היעד (USER / WORD / SETTING)
- `targetId` - מזהה היעד
- `details` - פרטים נוספים (JSON - מצב לפני ואחרי)
- `createdAt` - תאריך

### טבלת WordSession (סשן הגשת מילה)
- `id` - מזהה (UUID)
- `userId` - FK למשתמש
- `letter` - האות שהוגרלה
- `startedAt` - זמן תחילת הסשן
- `expiresAt` - זמן תפוגת הסשן (startedAt + 60 שניות)
- `used` - האם הסשן נוצל (boolean, default: false)

### טבלת SiteSettings (הגדרות מערכת)
- `id` - מזהה
- `key` - מפתח הגדרה
- `value` - ערך
- `updatedAt` - תאריך עדכון

---

## דפי האתר ומסכים

### 1. דף נחיתה (`/`)
- כותרת ברוכים הבאים לשקדול
- הסבר קצר ופשוט (ידידותי לילדים)
- דוגמאות מילים מאושרות (יסופקו ע"י הלקוח)
- סטטיסטיקות: כמה מילים במילון, כמה משתמשים
- כפתור "בואו נמציא מילה!" → הרשמה/כניסה
- כפתור "צפו במילון" → דף המילון
- קישורים לדף תקנון ודף אודות

### 2. דף הרשמה/כניסה (`/auth`)
- טופס הרשמה: שם מלא, דוא"ל, גיל
- כניסה עם דוא"ל (magic link / OTP - בלי סיסמה, פשוט לילדים)
- הודעה: "השימוש חינם לגמרי!"

### 3. דף המצאת מילה (`/create`)
- הגרלת אות עברית אקראית (מוצגת בגדול)
- טיימר 60 שניות (ויזואלי, עם אנימציה)
- שדה מילה: מקסימום 12 אותיות, חייבת להתחיל באות שהוגרלה
- שדה משמעות: מקסימום 2 משפטים
- ולידציה בזמן אמת:
  - אורך מקסימלי 12 אותיות
  - תווים עבריים בלבד (א-ת)
  - אותיות סופיות (ם, ן, ף, ץ, ך) מותרות רק בסוף המילה
  - בדיקת כפילויות: האם המילה כבר קיימת במילון (בזמן אמת)
- כפתור "שלחו את המילה!"
- אימות טיימר בצד השרת (WordSession) למניעת עקיפה
- בדיקת הגבלת קצב: מקסימום הגשות ליום למשתמש
- בדיקת LLM לתוכן פוגעני לפני שמירה
- בדיקת LLM ל"ניתנת לביטוי" - האם המילה הגיונית פונטית

### 4. דף המילון (`/dictionary`)
- רשימת כל המילים המאושרות
- סינון לפי אות, חיפוש חופשי
- תצוגת כרטיסיות: מילה, משמעות, מי המציא

### 5. דף פרופיל משתמש (`/profile`)
- המילים שהמשתמש הציע
- סטטוס כל מילה (ממתינה/מאושרת/נדחתה)
- סטטיסטיקות אישיות
- פעמון התראות: מילים שאושרו/נדחו, אזהרות

### 6. דף תקנון (`/terms`)
- תקנון השימוש באתר בשפה פשוטה וברורה (מותאם לילדים)
- כללי ההתנהגות בקהילה
- מדיניות תוכן: מה מותר ומה אסור
- מדיניות אזהרות, השעיות וחסימות
- הגנת פרטיות ושימוש במידע אישי
- זכויות יוצרים על מילים שנוצרו
- המשתמש חייב לאשר את התקנון בעת ההרשמה

### 7. דף אודות (`/about`)
- הסיפור מאחורי שפת השקדול
- הצגת ועדת השפה בראשות גב' שקד
- החזון: שפה קהילתית שנבנית יחד
- איך התהליך עובד (הסבר מאויר ופשוט)
- שאלות נפוצות (FAQ)
- יצירת קשר

### 8. דף אדמין (`/admin`) - מוגן
- **ניהול מילים**: אישור/דחייה של מילים ממתינות, עריכה, מחיקה
- **ניהול משתמשים**: צפייה, השעיה, ביטול, שינוי סטטוס
- **לוג ניטור**: צפייה בניסיונות תוכן פוגעני
- **הגדרות מערכת**: שינוי מגבלות (טיימר, אורך מילה, אורך הסבר)
- **סטטיסטיקות**: גרפים, מילים ליום, משתמשים חדשים
- **ניהול אותיות**: הגדרת אותיות פעילות/לא פעילות
- **לוג פעולות אדמין**: מעקב מלא אחר כל פעולה שבוצעה (מי, מתי, מה)

---

## לוגיקת Content Moderation (ניטור תוכן)

### זרימת בדיקה מלאה
```
משתמש לוחץ "בואו נמציא מילה!"
       ↓
יצירת WordSession בצד השרת (letter, startedAt, expiresAt)
       ↓
הצגת אות + טיימר 60 שניות (צד לקוח + צד שרת)
       ↓
משתמש שולח מילה + משמעות
       ↓
בדיקה 1: אימות WordSession (לא פג תוקף, לא נוצל)
       ↓
בדיקה 2: הגבלת קצב (לא עבר מכסה יומית)
       ↓
בדיקה 3: ולידציה בסיסית:
  - אורך מילה (עד 12 אותיות)
  - תווים עבריים בלבד
  - אותיות סופיות רק בסוף מילה
  - אורך משמעות (עד 2 משפטים)
  - המילה מתחילה באות שהוגרלה
       ↓
בדיקה 4: כפילויות - המילה לא קיימת כבר ב-DB
       ↓
בדיקה 5: Gemini - ביטוי (האם ניתן לבטא את המילה?)
  └─ לא ניתנת לביטוי → הודעה ידידותית, אפשרות לנסות שוב
       ↓
בדיקה 6: Gemini - תוכן (האם התוכן פוגעני?)
  ┌─ תוכן תקין → שמירה ב-DB כ-PENDING + התראה "המילה נשלחה לאישור!"
  └─ תוכן פוגעני → בדיקת היסטוריה:
       ├─ פעם ראשונה → אזהרה (warningCount = 1) + התראה
       ├─ פעם שנייה → השעיה 24 שעות (warningCount = 2) + התראה
       ├─ פעם שלישית → השעיה 10 ימים (warningCount = 3) + התראה
       └─ פעם רביעית → חסימה לצמיתות (status = BANNED) + התראה
       ↓
  סימון WordSession כ-used
  רישום ב-ModerationLog (במקרה של תוכן פוגעני)
  רישום ב-AdminAuditLog (פעולות אוטומטיות)
```

### Prompt ל-Gemini - בדיקת תוכן
```
אתה מנגנון סינון תוכן לאתר ילדים בעברית.
בדוק אם הטקסט הבא מכיל:
- ביטויים גזעניים
- הפליה בין המינים
- כינויי גנאי
- שמות או תיאורים פוגעניים
- תוכן לא חינוכי או לא ראוי לילדים

טקסט לבדיקה:
מילה: "{word}"
משמעות: "{meaning}"

החזר תשובה בפורמט JSON בלבד:
{"safe": true/false, "reason": "סיבה בעברית"}
```

### Prompt ל-Gemini - בדיקת ביטוי (pronounceability)
```
אתה בודק אם מילה בעברית ניתנת לביטוי.
מילה ניתנת לביטוי אם:
1. יש בה שילוב של עיצורים ותנועות שאפשר להגות
2. אין בה רצף של יותר מ-3 עיצורים ללא תנועה
3. היא לא מורכבת מאותיות אקראיות חסרות הגיון פונטי

המילה: "{word}"

החזר תשובה בפורמט JSON בלבד:
{"pronounceable": true/false, "suggestion": "הסבר קצר בעברית"}
```

---

## שלבי פיתוח (סדר עבודה)

### שלב 1: תשתית פרויקט
- [ ] אתחול Next.js עם TypeScript
- [ ] הגדרת Tailwind CSS עם RTL
- [ ] הגדרת Prisma עם PostgreSQL
- [ ] מבנה תיקיות ופריסה בסיסית
- [ ] הגדרת משתני סביבה (.env.example)

### שלב 2: בסיס נתונים
- [ ] כתיבת Prisma Schema מלא
- [ ] יצירת migration ראשוני
- [ ] Seed data (דוגמאות, אדמין ראשון)

### שלב 3: אימות משתמשים (Auth)
- [ ] הגדרת NextAuth.js עם Email Provider
- [ ] דף הרשמה (שם, דוא"ל, גיל)
- [ ] דף כניסה
- [ ] Middleware להגנה על דפים
- [ ] בדיקת סטטוס משתמש (השעיה/חסימה)

### שלב 4: ליבת המערכת - המצאת מילים
- [ ] API route ליצירת WordSession (הגרלת אות + טיימר צד-שרת)
- [ ] קומפוננטת הגרלת אות
- [ ] קומפוננטת טיימר (60 שניות, מסונכרן עם צד-שרת)
- [ ] טופס הגשת מילה עם ולידציות (כולל אותיות סופיות, כפילויות בזמן אמת)
- [ ] API route לשמירת מילה (עם אימות WordSession)
- [ ] הגבלת קצב יומית למשתמש
- [ ] אינטגרציה עם Gemini לבדיקת תוכן פוגעני
- [ ] אינטגרציה עם Gemini לבדיקת ביטוי (pronounceability)
- [ ] לוגיקת אזהרות והשעיות
- [ ] יצירת התראות אוטומטיות (Notifications)

### שלב 5: דף המילון
- [ ] תצוגת מילים מאושרות
- [ ] סינון לפי אות
- [ ] חיפוש חופשי
- [ ] עימוד (pagination)

### שלב 6: דף נחיתה
- [ ] עיצוב דף ראשי
- [ ] אזור דוגמאות (placeholder עד קבלת תוכן)
- [ ] סטטיסטיקות חיות

### שלב 7: פאנל אדמין
- [ ] Layout אדמין מוגן
- [ ] ניהול מילים (אישור/דחייה/מחיקה) + יצירת התראות למשתמש
- [ ] ניהול משתמשים (צפייה/השעיה/חסימה)
- [ ] לוג ניטור תוכן
- [ ] לוג פעולות אדמין (AdminAuditLog) - מעקב מלא
- [ ] הגדרות מערכת
- [ ] דשבורד סטטיסטיקות

### שלב 8: דפי תוכן (תקנון ואודות)
- [ ] דף תקנון עם תוכן בשפה פשוטה
- [ ] דף אודות עם הסיפור, החזון, FAQ
- [ ] checkbox אישור תקנון בטופס ההרשמה

### שלב 9: פרופיל משתמש והתראות
- [ ] דף פרופיל אישי
- [ ] היסטוריית מילים
- [ ] סטטיסטיקות אישיות
- [ ] מערכת התראות (פעמון + רשימת התראות)

### שלב 10: ליטוש ונגישות
- [ ] בדיקת נגישות (ARIA, ניווט מקלדת, ניגודיות)
- [ ] שפה פשוטה וברורה בכל הממשק
- [ ] Responsive design (מובייל)
- [ ] הודעות שגיאה ידידותיות לילדים

### שלב 11: Deploy ל-Railway
- [ ] הגדרת Railway project
- [ ] חיבור PostgreSQL
- [ ] הגדרת משתני סביבה
- [ ] קובץ railway.json / Procfile
- [ ] בדיקה סופית בסביבת production

---

## מבנה תיקיות מוצע
```
shakdol/
├── prisma/
│   ├── schema.prisma
│   ├── seed.ts
│   └── migrations/
├── src/
│   ├── app/
│   │   ├── layout.tsx          # Layout ראשי (RTL, פונטים)
│   │   ├── page.tsx            # דף נחיתה
│   │   ├── auth/
│   │   │   ├── signin/page.tsx
│   │   │   └── register/page.tsx
│   │   ├── create/
│   │   │   └── page.tsx        # המצאת מילה
│   │   ├── dictionary/
│   │   │   └── page.tsx        # המילון
│   │   ├── profile/
│   │   │   └── page.tsx        # פרופיל אישי
│   │   ├── terms/
│   │   │   └── page.tsx        # תקנון
│   │   ├── about/
│   │   │   └── page.tsx        # אודות
│   │   ├── admin/
│   │   │   ├── layout.tsx      # Layout אדמין
│   │   │   ├── page.tsx        # דשבורד
│   │   │   ├── words/page.tsx  # ניהול מילים
│   │   │   ├── users/page.tsx  # ניהול משתמשים
│   │   │   ├── logs/page.tsx   # לוג ניטור
│   │   │   └── settings/page.tsx
│   │   └── api/
│   │       ├── auth/[...nextauth]/route.ts
│   │       ├── words/
│   │       │   ├── route.ts          # CRUD מילים
│   │       │   ├── check/route.ts    # בדיקת כפילויות (GET)
│   │       │   └── session/route.ts  # יצירת WordSession
│   │       ├── moderation/route.ts
│   │       ├── notifications/route.ts
│   │       └── admin/
│   ├── components/
│   │   ├── ui/                 # כפתורים, inputs, כרטיסיות
│   │   ├── LetterWheel.tsx     # הגרלת אות
│   │   ├── Timer.tsx           # טיימר 60 שניות
│   │   ├── WordForm.tsx        # טופס הגשת מילה
│   │   ├── WordCard.tsx        # כרטיס מילה
│   │   └── Navbar.tsx          # תפריט ניווט
│   ├── lib/
│   │   ├── prisma.ts           # Prisma client
│   │   ├── auth.ts             # NextAuth config
│   │   ├── gemini.ts           # Gemini API client
│   │   ├── moderation.ts       # לוגיקת ניטור תוכן
│   │   ├── pronounce.ts        # בדיקת ביטוי מילה
│   │   ├── hebrew-validation.ts # ולידציות עברית (סופיות, תווים)
│   │   └── rate-limit.ts       # הגבלת קצב הגשות
│   └── types/
│       └── index.ts
├── public/
│   └── fonts/
├── .env.example
├── next.config.js
├── tailwind.config.ts
├── package.json
├── railway.json
└── README.md
```

---

## הגדרות ומגבלות ברירת מחדל
| הגדרה | ערך | ניתן לשינוי באדמין |
|--------|-----|-------------------|
| זמן להגשה | 60 שניות | כן |
| אורך מילה מקסימלי | 12 אותיות | כן |
| אורך משמעות מקסימלי | 2 משפטים | כן |
| אזהרה ראשונה | הודעה בלבד | לא |
| הפרה שנייה | השעיה 24 שעות | כן |
| הפרה שלישית | השעיה 10 ימים | כן |
| הפרה רביעית | חסימה לצמיתות | לא |
| מקסימום הגשות ליום | 10 מילים | כן |
| בדיקת ביטוי (Gemini) | פעיל | כן |

---

## ולידציות אותיות עבריות

### כללי אותיות סופיות
אותיות סופיות מותרות **רק** בסוף המילה:
| אות רגילה | אות סופית |
|-----------|-----------|
| מ | ם |
| נ | ן |
| פ | ף |
| צ | ץ |
| כ | ך |

ולידציה: אם נמצאה אות סופית שלא בסוף המילה → שגיאה ידידותית.

### תווים מותרים
- רק אותיות א-ת (כולל סופיות)
- ללא רווחים, מספרים, ניקוד, או סימנים

---

## נקודות חשובות
1. **נגישות**: כל הממשק בעברית פשוטה, מותאם לילדים
2. **RTL**: כל האתר מימין לשמאל
3. **בטיחות**: בדיקת LLM לכל תוכן לפני כניסה למערכת
4. **פרטיות**: לא שומרים סיסמאות, כניסה עם magic link
5. **חינמי**: אין תשלום, אין פרימיום
6. **דוגמאות**: מקום שמור בדף הנחיתה - הלקוח יספק
7. **אימות טיימר**: צד-שרת באמצעות WordSession, לא רק צד-לקוח
8. **הגבלת קצב**: מניעת ספאם עם מכסה יומית להגשות
9. **כפילויות**: unique constraint על מילים + בדיקה בזמן אמת
10. **התראות**: המשתמש מקבל עדכון כשמילה מאושרת/נדחית
11. **לוג אדמין**: מעקב מלא אחרי פעולות אדמין לשקיפות ובקרה
